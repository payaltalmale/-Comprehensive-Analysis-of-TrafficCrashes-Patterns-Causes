CREATE KEYSPACE IF NOT EXISTS traffic_data WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};

CREATE TABLE IF NOT EXISTS traffic_data.traffic_data_table2 (
    CRASH_RECORD_ID TEXT,
    POSTED_SPEED_LIMIT INT,
    TRAFFIC_CONTROL_DEVICE TEXT,
    DEVICE_CONDITION TEXT,
    WEATHER_CONDITION TEXT,
    LIGHTING_CONDITION TEXT,
    FIRST_CRASH_TYPE TEXT,
    TRAFFICWAY_TYPE TEXT,
    ALIGNMENT TEXT,
    ROADWAY_SURFACE_COND TEXT,
    ROAD_DEFECT TEXT,
    REPORT_TYPE TEXT,
    CRASH_TYPE TEXT,
    HIT_AND_RUN_I TEXT,
    DAMAGE TEXT,
    PRIM_CONTRIBUTORY_CAUSE TEXT,
    STREET_NO INT,
    STREET_DIRECTION TEXT,
    STREET_NAME TEXT,
    NUM_UNITS INT,
    MOST_SEVERE_INJURY TEXT,
    INJURIES_TOTAL INT,
    INJURIES_FATAL INT,
    INJURIES_INCAPACITATING INT,
    INJURIES_NO_INDICATION INT,
    INJURIES_UNKNOWN INT,
    CRASH_HOUR INT,
    CRASH_DAY_OF_WEEK INT,
    CRASH_MONTH INT,
    LATITUDE FLOAT,
    LONGITUDE FLOAT,
    YEAR INT,
    TIMESTAMP TEXT,
    PRIMARY KEY (CRASH_RECORD_ID)
);